// button to listen 
//loop using for eaxh loop then we should add listener for the buttons 
/*export function toPlay(){
    const buttons = document.querySelectorAll('rps')
    
    buttons.forEach(buttons => {
        buttons.onclick = () => getUserSelection(buttons.value)
    })
}*/